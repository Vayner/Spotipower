<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.enderwolf.spotipower" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.enderwolf.spotipower.app.AppController"
        android:allowBackup="true"
        android:icon="@drawable/ic_app"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MyActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".PlayerActivity"
            android:label="@string/title_activity_player"
            android:launchMode="singleInstance" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- An intent filter that will receive the response from the authentication service -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="callback"
                    android:scheme="spotipower-login" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NavDrawer"
            android:label="@string/title_activity_nav_drawer" >
        </activity>
        <activity
            android:name=".ListOfSearchedSongs"
            android:label="@string/title_activity_list_of_searched_songs" >
        </activity>
    </application>

</manifest>
